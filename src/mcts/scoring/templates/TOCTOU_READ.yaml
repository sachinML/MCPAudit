id: TOCTOU_READ
title: TOCTOU race between symlink check and read
severity: high
finding_class: security
min_confidence: 0.55
min_reachability: 0.25
max_depth: 5
exploit_cost: 4
anchor:
  first_edge: READS
layers:
  - dataflow
edge_pattern:
  - kind: READS
    from_kind: tool
  - kind: READS
    from_kind: tool
recommended_fixes:
  - atomic_open
  - disable_symlink_follow
explanation_steps:
  - "Filesystem tool reads paths that may race with symlink swaps."
  - "Parallel reads can observe inconsistent file state."
