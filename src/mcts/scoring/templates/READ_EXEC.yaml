id: READ_EXEC
title: Read then command execution chain
severity: critical
finding_class: security
min_confidence: 0.6
min_reachability: 0.25
max_depth: 5
exploit_cost: 3
anchor:
  first_edge: READS
layers:
  - dataflow
edge_pattern:
  - kind: READS
    from_kind: tool
  - kind: INVOKES
legacy_finding_id: chain-read-exec
explanation_steps:
  - "Tool reads untrusted input."
  - "Separate tool can execute commands influenced by that input."
recommended_fixes:
  - isolate_read_and_exec_tools
  - validate_inputs_before_exec
